import React from "react";
import ReactDOM from "react-dom";

import MicroscopeMetadataTool from "4dn-microscopy-metadata-tool";

window.onload = () => {
	ReactDOM.render(<MicroscopeMetadataTool />, document.getElementById("root"));
};
